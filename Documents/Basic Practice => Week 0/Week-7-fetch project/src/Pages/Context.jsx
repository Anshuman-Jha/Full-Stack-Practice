import { createContext } from "react";

//Created Context to Teleport that Variable=> Created Teleporter
//Provider =. Given a Integer Value you will Teleport Inialized with 0 
export const CountContext = createContext(0);

// Returns an Object That Has .Provider()